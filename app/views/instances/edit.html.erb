<div class="row">
  <div class="col-lg-12">
      <h1 class="page-header">Casos</h1>
  </div>
  <!-- /.col-lg-12 -->
</div>
<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-heading">
        <% if detective_signed_in? %>
      	 <%= link_to "Casos", detective_instances_path(current_detective.id) %> > Agregar Casos
        <% elsif admin_signed_in? %>
          <%= link_to "Casos", instances_index_path %> > Agregar Casos
        <% end %>
			</div>
      <div class="panel-body">
      	<div class="row">
      		<div class="col-lg-6">
            <% if detective_signed_in? %>
						  <%= render 'form' %>
            <% else %>
              <%= render 'form_admin' %>
            <% end %>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>