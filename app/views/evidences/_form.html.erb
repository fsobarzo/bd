<%= form_for [current_detective, @instance, @evidence] do |f| %>

  <div class="form-group">
   <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit 'Agregar', :class => 'btn btn-default' %>
  </div>
<% end %>