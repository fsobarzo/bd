<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">Armas</h1>
  </div>
  <!-- /.col-lg-12 -->
</div>
<!-- /.row -->
<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-heading">
        Listado de Armas
      </div>
      <!-- /.panel-heading -->
      <div class="panel-body">
        <div class="table-responsive">
          <table class="table table-striped table-bordered table-hover" id="tables">
            <thead>
              <tr>
                <th>Nombre</th>
                <th>Descripción</th>
                <th>Acciones</th>
              </tr>
            </thead>
            <tbody>
              <% @weapons.each do |weapon| %>
              <tr>
                <td><%= weapon.name %></td>
                <td><%= weapon.description %></td>
                <td>
                  <%= link_to 'Show', weapon, class: "btn btn-info" %>
                  <%= link_to 'Edit', edit_weapon_path(weapon), class: "btn btn-info" %>
                  <%= link_to 'Destroy', weapon, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
                </td>
              </tr>
    <% end %>
  </tbody>
</table>
